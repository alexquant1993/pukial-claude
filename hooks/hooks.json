{
  "description": "Auto-fix and format .dart files after Edit/Write using the FVM-pinned SDK. No-ops on non-.dart files, so it is inert in non-Flutter repos.",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/dart-format.sh\"",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
