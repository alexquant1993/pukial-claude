# HANDOFF - walkthrough: built and gated -> nothing pending

**Date:** 2026-09-08
**Deck:** `out/walk/deck.pptx` (generated; the gate rebuilds it, nobody edits it by hand)
**Work directory:** `out/walk/` - every generated file goes here

---

## 0. How to work here

These are defaults the author can override, not law. They decide whether an
agent loading this file is useful or destructive.

- **Propose text changes in the conversation**, in a small number of
  alternatives, and let the author apply them. Act on files only when told
  to, with an explicit go.
- **Never overwrite the author's deck.** Read one path, write another. The
  author's file is usually open in PowerPoint and locked, with a `~$`
  sidecar present; write to the work directory and say so, and do not
  retry against the lock.
- **Reading order before touching anything:** this handoff, then the spec,
  then the ledger, then dump the current deck's text
  (`scripts/dump_text.py --deck DECK --out WORK/text.txt`) and diff it
  against the last known dump, then look at the current PNGs. The dump and
  diff are what discover the author's manual edits; regenerating from a
  stale assumption destroys them.
- **One question per message, in prose**, with enough context to decide
  and the reasoning behind any recommendation.
- **Replacement copy more than 35% longer than the original run text is
  shortened, not allowed to overflow**, and every shortening is reported.
- **The intake before any deck.** Run `scripts/discover.py`, then ask four
  questions, one per message: which design system, whether the HTML draft is
  on (the default) or skipped with a reason, whether the deck carries
  speaker notes (`none`, the default; `pptx`, `teleprompter` or `both`), and
  what it ships as (`html`, `pptx` or `both`, the default). This deck's
  answers are on its spec's `Design system`, `HTML draft`, `Notes` and
  `Output` lines: the default brand, the draft skipped because section 5 is
  the design, notes in both outputs so the gate keeps proving them, and
  `Output: pptx` because a deck with no drafts has no HTML deck to build.

## 1. Where everything is

| Artifact | Path |
|---|---|
| Spec | `examples/walkthrough/spec.md` |
| Ledger | `examples/walkthrough/ledger.md` |
| Author's current deck | none - this deck is generated by the gate |
| Last text dump | `out/walk/text.txt` (written by the gate) |
| Builders | `examples/walkthrough/build_walkthrough.py` |
| Current PNGs | `out/png_walk/s01.png`, `out/png_walk/s02.png` |

Pipeline: see spec section 6.

## 2. Deck structure

1 -> (no number) -> Cover. 2 -> 2 -> The loop. Both generated; none hidden.

## 3. Decisions closed - do not relitigate

- The cover is exempt from the page number, the copyright line and the kicker (ledger, 2026-09-08; cost if wrong: remove the exemption and rebuild).
- The aside is notes-only (ledger, 2026-09-08; cost if wrong: caught by the lint step).

## 4. Changes agreed in conversation that the author said they would apply by hand

None. This deck has no hand-edited version.

## 5. What the next session does

1. Nothing for this deck. Use it as the pattern for a real one: copy `templates/spec.md`, fill section 3 before anything else.

## 6. Open flags

None.
